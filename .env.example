# Copia este archivo como `.env` en la raíz del proyecto
# Si no tienes backend, deja VITE_USE_MOCK=true para usar un mock con localStorage
VITE_USE_MOCK=true

# Cuando tengas backend, coloca su URL base y pon VITE_USE_MOCK=false
# VITE_API_URL=http://localhost:3000
